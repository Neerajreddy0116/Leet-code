class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        summ=0
        new=[]
        for i in digits:
            summ=summ*10+i
        plusone=summ+1
        for i in str(plusone):
            new.append(int(i))
        return new
